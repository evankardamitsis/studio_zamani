# Studio Zamani logo

Drop your logo file here, for example:

- `logo.svg` (preferred)
- `logo.png`

The coming soon page loads `/logo/logo.svg` by default. If that file is missing, the site shows a text fallback: **Studio Zamani**.
